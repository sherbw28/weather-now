<h1>id: <%= @weather.id %>の編集ページ</h1>

<%= form_with(model: @weather, local: true) do |f| %>
  <%= f.label :content, "天気" %>
  <%= f.text_field :content %>
  
  <%= f.submit "提出" %>
<% end %>

<%= link_to "一覧に戻る", root_path %>